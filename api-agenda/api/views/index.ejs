<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <%- include('partials/head') %>
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <header>
        <%- include('partials/nav') %>
    </header>

    <section class="conteudo">
        <div class="container-conteudo">
            <div class="cabecariosecundario">
                <input type="button" value="Tela Cheia" class="btn_expandir">

                <div class="" id="quantidade">
                    <p class="local">Quantitativo: 145 Alunos</p>
                </div>
            </div>

            <div class="fundo">         
                <div id="data"></div>
                <div class="turmas">
                    <div id="painelGeral">
                        <%salas.forEach(sala =>{%>
                            <div class="painelTurmas">
                                <p class="titulo"><%= sala.nome%></p>
                                <%eventos.forEach(evento =>{%>
                                    <% if(evento.sala_id == sala.id){%>
                                        <div class="salas" id="sala1">
                                            <p class="titulo" style="background-color:<%= evento.cor%>"><%= evento.turma%></p><br>
                                            <p class="callcenter"><%= evento.disciplina%></p>                             
                                            <p><%= evento.horario_inicio%>h - <%= evento.horario_fim%>h</p>
                                            <p>Total Alunos: <%= evento.qtd_alunos%></p><br>
                                            <div class="imgprofessor">
                                                <img src="/imagens/user.png" class="imagem">
                                                <p><%= evento.professor%></p>
                                            </div>
                                        </div>
                                    <%}%>
                                <%})%>
                            </div>
                        <% }) %>
                    </div>   
                </div>
            </div>
        </div>
    </section>
</body>
<script src="/js/index.js"></script>
</html>